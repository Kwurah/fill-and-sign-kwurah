services:
  - type: web
    name: fill-and-sign-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGO_URI
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
